# PMU Lip Color Match Pro — requirements
#
# 변경 사항:
# - opencv-python -> opencv-python-headless : GUI 종속성이 없어 서버/Streamlit Cloud/Docker
#   배포 환경에서 libGL 관련 오류 없이 설치됩니다.
# - 각 패키지에 최소 버전을 명시해 재현성을 높였습니다 (완전 고정 대신 호환 가능한 최소 버전).
# - pandas는 CSV 기반 색소 데이터베이스 업로드 기능에 사용됩니다.

streamlit>=1.32,<2.0
numpy>=1.24,<2.0
scikit-image>=0.22,<0.25
Pillow>=10.0,<11.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
scikit-learn>=1.3,<2.0
opencv-python-headless>=4.9,<5.0
